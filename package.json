{
  "name": "the-depth-chart",
  "version": "1.0.0",
  "private": true,
  "description": "Static site + Vercel functions. This package.json exists only so the dynamic OG-image endpoint (api/og-transfer-image) can use @vercel/og. No build step — static files and the plain Node functions in /api deploy unchanged.",
  "dependencies": {
    "@vercel/og": "0.9.0"
  }
}
