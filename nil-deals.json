{
  "_note": "Client-side known NIL deals. INTENTIONALLY EMPTY: by policy we do not publish reported real-deal salaries — every value on the site is the model's own estimate. The mechanism is retained (tdc-nil.js loads this at runtime, keyed by exact name → $M) but must stay empty unless the policy changes.",
  "deals": {}
}
