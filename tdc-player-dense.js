/* ============================================================
   TDC PLAYER DENSE JS — retired 2026.
   Previously force-locked the player page to dark (the dense reskin
   was dark-only, via a dark() call + a MutationObserver that reverted
   any theme change). The 2026 clean skin (tdc-skin.css) supports BOTH
   light and dark, so the theme is now owned solely by tdc-theme.js
   (saved choice, defaults light). No-op kept so the existing <script>
   reference doesn't 404.
   ============================================================ */
