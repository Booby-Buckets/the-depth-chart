{
  "_note": "Manual NIL overrides for things the model can't see. Keyed by exact player name. pos = NIL valuation position (e.g. a natural PF listed at SF for his lineup) — used for the model position multiplier AND the market curve, WITHOUT changing his depth-chart position. NOTE: hard $ (`value`) overrides are DEPRECATED — by policy we no longer publish reported real-deal salaries, so every player is priced by the model. Do not re-add `value` keys. (The baked nil-data.json may still carry override:true rows from a prior build; tdc-nil.js recomputes those from the model at runtime.)",
  "by_name": {
    "Thomas Haugh": { "pos": "PF", "note": "natural PF playing SF at Florida — value on his natural position" }
  }
}
